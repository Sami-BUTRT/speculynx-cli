openapi: 3.0.0
info:
  title: Expiring Key API
  version: 1.0.0
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key
      description: API key expires after 24 hours and must be rotated.
security:
  - ApiKeyAuth: []
paths:
  /items:
    get:
      responses:
        "200":
          description: OK
